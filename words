#include <stdio.h>
#include<string.h>
void main()
{

    char a[200];
    printf("Enter the sentence");
    gets(a);
    int len=strlen(a);
    int count=0;
    for(int i=0;i<len;i++)
    {
        if(a[i]==' ')
        count=count+1;
    }
    printf("The number of words is %d",count+1);
}
output:-
Enter the sentencedeekshitha lahari akhila
The number of words is 3
Process returned 24 (0x18)   execution time : 46.093 s
Press any key to continue.

